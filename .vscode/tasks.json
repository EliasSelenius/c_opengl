{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "${workspaceFolder}/build.bat"
        },
        {
            "label": "run",
            "type": "shell",
            "command": "${workspaceFolder}/bin/program.exe"
        }
    ]
}